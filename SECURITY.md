# Security Policy

- Report vulnerabilities privately to maintainers.
- Never commit secrets; use environment variables.
- Rotate tokens regularly; strip secrets from logs.
- For generated repos: follow the same rules; CI must not expose secrets.
